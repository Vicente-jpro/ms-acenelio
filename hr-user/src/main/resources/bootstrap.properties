spring.application.name=hr-user
server.port=${PORT:0}

eureka.instance.instance-id=${spring.application.name}:${spring.application.instance_id:${random.value}}

eureka.client.service-url.defaultZone=http://localhost:8761/eureka



# Configuraçao do profile apartir do servidor de configuracao hr-config-server 
# Os dados de configuracao estao a ser carregados apartir do meu GitHub no repositorio ms-course-config 
spring.cloud.config.uri=http://localhost:8888
spring.profiles.active=test
management.endpoints.web.exposure.include=*

# Database configuration
# spring.jpa.database=POSTGRESQL
# spring.datasource.platform=postgres
# spring.datasource.url=jdbc:postgresql://localhost:5432/db_hr_user
# spring.datasource.username=postgres
# spring.datasource.password=${POSTGRES_VICENTE_PASSWORD}
# spring.jpa.show-sql=true
# spring.jpa.generate-ddl=true
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

